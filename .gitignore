# ── Secrets (NEVER commit these) ─────────────────────────
.env
.env.development
.env.production
.env.staging
!.env.example

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.venv/
venv/

# ── Database files ────────────────────────────────────────
*.db
data/*.db
data/backups/
data/
*.sqlite3

# ── Logs ─────────────────────────────────────────────────
logs/*.log
logs/*.log.*

# ── Uploads ──────────────────────────────────────────────
data/uploads/

# ── OS / Editor ──────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# ── Runtime logs from launcher scripts (contain seeded credentials) ──
startup.log
startup_err.log
logs/*.log*

# Runtime logs in subdirectories — "logs/*.log" does not match logs/backend/*.log
logs/**/*.log
logs/**/*.log.*

# Reference media for the Easy Visit screen — large binaries, kept local
docs/reference/
