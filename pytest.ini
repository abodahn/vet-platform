[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    postgres: needs a real PostgreSQL database; auto-skipped unless TEST_POSTGRES_DSN is set
